import 'package:flutter/material.dart';

class MinisterHomeScreen extends StatelessWidget {
  const MinisterHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('VIP Home Screen'),
      ),
    );
  }
}
